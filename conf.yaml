#intents-descriptive integer
intents: 8589937728

#command prefix
prefix: "!"

#channel id in which to send messages when status changes
notification_channel: 

#roles that can use admin commands
allowed_roles: []

#file path for logging infos
logfile: 

ACTIVITY_STRING:
  OPEN: "[local OUVERT]"
  CLOSED: "[local FERMÉ]"

bot_token:

status_cache_filepath: "status_cache.yaml"